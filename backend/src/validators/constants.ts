/**
 * Shared domain constants for closed-set validation.
 * Re-exported from @renovation/shared-types (single source of truth).
 */
export {
  PRODUCT_CATEGORIES,
  ROOM_TYPES,
  STYLE_SLUG_REGEX,
  type ProductCategory,
  type RoomType,
} from '@renovation/shared-types';

/** Maximum number of image attachments per message */
export const MAX_ATTACHMENTS = 5;
